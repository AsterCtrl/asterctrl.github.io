# AsterCtrl Documentation

Chinese-first, i18n-ready documentation for the distributed control framework.

```sh
npm install
npm run build
npm start
```

The site builds all configured locales. Documentation describes implemented
behavior only when a linked test, generated report, or build artifact supports the
claim.
